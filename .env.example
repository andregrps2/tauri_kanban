# This file is an example. Create a .env.local file and add your actual credentials there.
# These variables are exposed to the browser, so they must be prefixed with NEXT_PUBLIC_.
NEXT_PUBLIC_CLICKUP_API_TOKEN=your_clickup_api_token_here
NEXT_PUBLIC_CLICKUP_LIST_ID=your_clickup_list_id_here
